# C API

The public C API is declared in `api/c/include/drava/drava_c.h`. It is the
interface the SWIG Python bindings wrap, and can be used directly from C/C++.

```{doxygenfile} drava/drava_c.h
:project: drava
```
