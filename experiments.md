# Drava Paper Experiments

This index has moved to **[docs/paper.md](docs/paper.md)**, which lists every
reproducible paper experiment with its driver, logs, figure package, submitted
figure, and the exact benchmark command.

Experiment drivers still live in `experiments/`, application code under
`examples/`, captured logs in `experiments/logs/`, figure packages in
`experiments/figures/`, and final figures in `figs/paper_figs/`.
